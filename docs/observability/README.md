# Observability

Store scenario telemetry plans and QA diagnostics here. Current contract evidence is in `tests/project-contract.test.ts`.

Study-data field reference, capture profiles, the analysis platform, and the metrics catalog now live under [`docs/study/`](../study/) — the study-data hub.
