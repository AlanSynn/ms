# Observability

Store scenario telemetry plans and QA diagnostics here. Current contract evidence is in `tests/project-contract.test.ts`.

- [`study-telemetry-data-dictionary.md`](study-telemetry-data-dictionary.md) — canonical field reference for every `/ms-study/v1` event, the snapshot schema, capture profiles, PII controls, and the replay projection. If a field is not listed there it is not emitted.
