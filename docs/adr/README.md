# Architecture Decision Records

Store durable architecture decisions here. Current decision: web runtime uses bundled npm dependencies with no CDN import maps, and shipped browser/Tauri builds exclude image-recognition models and runtimes.
